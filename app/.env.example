# Cloud Run (or local) URL of the AI proxy. Provider keys never go here.
# Example after deploy: https://emcure-ai-proxy-xxxxx-uc.a.run.app
VITE_AI_PROXY_URL=http://localhost:8080

# Dedicated EM-CURE Supabase project (Free tier). Anon key is public; RLS is the boundary.
# Leave blank to keep designs in this browser only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
